{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Sniffer",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/sniffer.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "preLaunchTask": "build-sniffer"
        }
    ]
}
